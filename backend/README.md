# 周文王占卜系统后端服务 🔮

这是专门为周文王占卜系统设计的后端API服务，主要用于安全地与Gemini API交互，并提供**流式响应**功能，让用户在占卜过程中感受到实时的"对话体验"。

## ✨ 核心特性

- 🌟 **流式AI响应** - 实时显示占卜分析过程，模拟真实对话
- 🔒 **API密钥安全** - API密钥完全隐藏在后端，前端无法获取
- 🚀 **高性能** - 支持并发请求，稳定可靠
- 📱 **跨平台兼容** - 支持Web和移动端访问
- 🌐 **局域网友好** - 一台服务器，多人共享使用

## 🚀 快速开始

### 1. 安装依赖

```bash
cd backend
npm install
```

### 2. 配置环境变量

```bash
# 复制示例配置文件
cp env.example .env

# 编辑配置文件
nano .env  # 或使用你喜欢的编辑器
```

在 `.env` 文件中设置：
```env
# 必需：你的Gemini API密钥
GEMINI_API_KEY=AIzaSyC...你的API密钥...

# 可选：服务器端口（默认3001）
PORT=3001

# 可选：允许的前端域名
ALLOWED_ORIGINS=http://localhost:5173,http://192.168.1.*
```

### 3. 启动服务

```bash
# 方式一：使用启动脚本（推荐）
chmod +x start.sh
./start.sh

# 方式二：直接启动
npm start

# 方式三：开发模式（自动重启）
npm run dev
```

### 4. 验证服务

打开浏览器访问：`http://localhost:3001/api/health`

如果看到以下响应，说明服务启动成功：
```json
{
  "status": "ok",
  "timestamp": "2024-01-15T...",
  "apiConfigured": true,
  "version": "1.0.0"
}
```

## 🚀 PM2 进程管理

本项目支持使用 PM2 进行进程管理，提供更稳定的生产环境部署和开发环境管理。

### PM2 安装与配置

PM2 已经作为开发依赖包含在项目中，无需额外安装。

### 环境配置文件

项目包含两个独立的PM2配置文件：

- `ecosystem.dev.config.js` - 开发环境配置（单实例，支持热重载）
- `ecosystem.prod.config.js` - 生产环境配置（4实例集群模式）

### PM2 命令参考

#### 🔧 基本操作

```bash
# 启动开发环境（单实例，支持文件监听）
npm run pm2:dev

# 启动生产环境（4实例集群模式）
npm run pm2:prod

# 查看所有进程状态
npx pm2 list

# 查看实时日志
npm run pm2:logs

# 实时监控面板
npm run pm2:monit
```

#### ⏹️ 停止服务

```bash
# 停止开发环境
npm run pm2:stop:dev

# 停止生产环境
npm run pm2:stop:prod

# 停止所有PM2进程
npx pm2 stop all
```

#### 🔄 重启服务

```bash
# 重启开发环境
npm run pm2:restart:dev

# 重启生产环境
npm run pm2:restart:prod

# 重启所有进程
npx pm2 restart all
```

#### 🗑️ 删除进程

```bash
# 删除开发环境进程
npm run pm2:delete:dev

# 删除生产环境进程
npm run pm2:delete:prod

# 删除所有进程
npx pm2 delete all

# 完全清理PM2（包括守护进程）
npx pm2 kill
```

#### 📊 监控与日志

```bash
# 查看进程详细信息
npx pm2 show zhouwenwang-dev
npx pm2 show zhouwenwang-prod

# 查看实时日志（带行数限制）
npx pm2 logs --lines 50

# 查看错误日志
npx pm2 logs --err

# 清空日志
npx pm2 flush

# 保存当前进程列表
npx pm2 save

# 开机自启动（需要管理员权限）
npx pm2 startup
```

### 环境配置对比

| 配置项 | 开发环境 | 生产环境 |
|--------|----------|----------|
| 实例数 | 1个 | 4个（集群） |
| 执行模式 | fork | cluster |
| 文件监听 | ✅ 启用 | ❌ 禁用 |
| 端口 | 3001 | 3001 |
| 内存限制 | 200MB | 500MB |
| 日志文件 | dev-*.log | prod-*.log |
| 自动重启 | ✅ | ✅ 高级配置 |

### 并发能力

#### 开发环境（1个实例）
- **AI请求并发**: 20-50个
- **轻量请求并发**: 1000-3000/秒
- **内存使用**: 50-200MB
- **适用场景**: 开发调试、小规模测试

#### 生产环境（4个实例集群）
- **AI请求并发**: 80-200个（4倍提升）
- **轻量请求并发**: 4000-12000/秒（4倍提升）
- **内存使用**: 200-800MB
- **适用场景**: 生产部署、高并发访问

### 日志文件位置

PM2 会在 `logs/` 目录下创建以下日志文件：

```
logs/
├── dev-err.log        # 开发环境错误日志
├── dev-out.log        # 开发环境输出日志（包含访问日志）
├── dev-combined.log   # 开发环境合并日志
├── prod-err.log       # 生产环境错误日志
├── prod-out.log       # 生产环境输出日志
└── prod-combined.log  # 生产环境合并日志
```

### 访问日志格式

应用会记录详细的访问日志，格式如下：

```
[2025-06-25 16:15:45 CST] GET  /api/health     - 192.168.1.100   - Win10 Chrome/137
[2025-06-25 16:15:52 CST] POST /api/gemini/stream - 10.10.9.123  - Win10 Chrome/137
```

### 故障排除

#### 端口占用错误 (EADDRINUSE)
```bash
# 检查端口占用
netstat -ano | findstr :3001

# 强制停止所有Node进程（谨慎使用）
taskkill /f /im node.exe

# 重新启动
npx pm2 kill
npm run pm2:dev
```

#### 进程启动失败
```bash
# 查看详细错误信息
npx pm2 logs --err --lines 20

# 检查配置文件语法
node -c ecosystem.dev.config.js
node -c ecosystem.prod.config.js
```

#### 内存泄漏监控
```bash
# 启用内存监控
npx pm2 monit

# 查看内存使用趋势
npx pm2 list
```

### 推荐使用方式

1. **开发阶段**: 使用 `npm run pm2:dev` 启动单实例，便于调试
2. **测试阶段**: 使用 `npm run pm2:prod` 测试集群模式性能
3. **生产部署**: 使用 `npm run pm2:prod` 获得最佳并发性能
4. **日志监控**: 定期查看 `npm run pm2:logs` 了解系统状态

## 📡 API 端点

### 核心端点

| 端点 | 方法 | 描述 | 特色 |
|------|------|------|------|
| `/api/health` | GET | 健康检查 | 验证服务状态 |
| `/api/gemini/stream` | POST | **🌟 流式文本生成** | **推荐使用** |
| `/api/gemini/generate` | POST | 标准文本生成 | 备用方案 |
| `/api/gemini/vision` | POST | 图像分析 | 手相等 |
| `/api/gemini/vision-stream` | POST | 流式图像分析 | 手相流式 |
| `/api/validate` | GET | API密钥验证 | 测试配置 |

### 🌟 流式API使用示例

**六爻占卜流式分析：**
```javascript
// 调用流式API
fetch('http://localhost:3001/api/gemini/stream', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
  },
  body: JSON.stringify({
    contents: [
      {
        parts: [{ text: "请分析这个六爻卦象..." }]
      }
    ]
  })
})
.then(response => {
  const reader = response.body.getReader();
  const decoder = new TextDecoder();
  
  function readStream() {
    return reader.read().then(({ done, value }) => {
      if (done) return;
      
      const chunk = decoder.decode(value);
      // 处理服务器发送的事件
      const lines = chunk.split('\n');
      
      for (const line of lines) {
        if (line.startsWith('data: ')) {
          const data = JSON.parse(line.slice(6));
          if (data.text) {
            // 实时更新UI显示
            updateDivinationText(data.text);
          }
        }
      }
      
      return readStream();
    });
  }
  
  return readStream();
});
```

## 🔧 配置说明

### 环境变量

| 变量 | 必需 | 默认值 | 说明 |
|------|------|--------|------|
| `GEMINI_API_KEY` | ✅ | - | Gemini API密钥 |
| `PORT` | ❌ | 3001 | 服务器端口 |
| `NODE_ENV` | ❌ | development | 运行环境 |
| `ALLOWED_ORIGINS` | ❌ | localhost:5173 | 允许的前端域名 |
| `MAX_REQUESTS_PER_MINUTE` | ❌ | 60 | API调用频率限制 |
| `MAX_FILE_SIZE` | ❌ | 10485760 | 文件上传大小限制 |

### CORS配置

后端默认允许以下域名访问：
- `http://localhost:5173` - 本地开发
- `http://127.0.0.1:5173` - 本地回环
- `http://192.168.1.*` - 局域网通配符

如需修改，请编辑 `ALLOWED_ORIGINS` 环境变量。

## 🌐 局域网部署

### 1. 获取服务器IP地址

```bash
# Linux/macOS
ip addr show | grep inet | grep -v 127.0.0.1

# Windows
ipconfig | findstr "IPv4"
```

### 2. 启动后端服务

```bash
cd backend
PORT=3001 npm start
```

### 3. 部署前端

```bash
# 构建前端
cd zhouwenwang-divination
npm run build

# 使用简单HTTP服务器托管
npx serve dist -p 3000

# 或使用Python
python3 -m http.server 3000 -d dist
```

### 4. 访问方式

- **后端API**: `http://192.168.1.100:3001`
- **前端界面**: `http://192.168.1.100:3000`

局域网内其他设备可以通过这些地址访问占卜系统。

## 🔒 安全特性

### API密钥保护
- ✅ API密钥完全存储在服务器端
- ✅ 前端代码中不包含任何敏感信息
- ✅ 所有Gemini API调用通过后端代理

### 访问控制
- ✅ CORS配置限制访问来源
- ✅ 请求频率限制防止滥用
- ✅ 文件上传大小限制

### 错误处理
- ✅ 详细的错误日志记录
- ✅ 优雅的错误响应
- ✅ 自动重试机制

## 📊 性能监控

### 日志信息
服务器会输出详细的请求日志：
```
[2024-01-15T10:30:15.123Z] POST /api/gemini/stream - 192.168.1.105
正在建立流式连接: gemini-2.5-flash-lite-preview-06-17
流式响应完成，原因: STOP
```

### 健康检查
定期访问 `/api/health` 端点监控服务状态：
```bash
curl http://localhost:3001/api/health
```

## 🚨 故障排除

### 常见问题

**1. API密钥错误**
```
❌ GEMINI_API_KEY 格式不正确，应该以 AIza 开头
```
解决：检查 `.env` 文件中的API密钥格式。

**2. CORS错误**
```
Access to fetch at 'http://...' from origin 'http://...' has been blocked by CORS policy
```
解决：在 `ALLOWED_ORIGINS` 中添加前端域名。

**3. 流式连接失败**
```
流式API调用失败: Error: timeout of 60000ms exceeded
```
解决：检查网络连接和Gemini API状态。

### 调试模式

启用详细日志：
```bash
LOG_LEVEL=debug npm start
```

## 📋 开发说明

### 目录结构
```
backend/
├── server.js              # 主服务器文件
├── package.json           # 依赖配置
├── env.example            # 环境变量示例
├── start.sh               # 启动脚本
├── frontend-integration.js # 前端集成示例
└── README.md              # 本文档
```

### 开发依赖
```bash
npm install -D nodemon
npm run dev  # 自动重启服务器
```

### API测试
```bash
# 测试健康检查
curl http://localhost:3001/api/health

# 测试标准API
curl -X POST http://localhost:3001/api/gemini/generate \
  -H "Content-Type: application/json" \
  -d '{"contents":[{"parts":[{"text":"测试消息"}]}]}'

# 测试流式API（需要支持SSE的客户端）
curl -N http://localhost:3001/api/gemini/stream \
  -H "Content-Type: application/json" \
  -d '{"contents":[{"parts":[{"text":"测试流式消息"}]}]}'
```

## 🎯 前端集成

参考 `frontend-integration.js` 文件中的示例代码，将现有的直接Gemini API调用替换为后端调用。

主要修改点：
1. 将API调用地址改为后端地址
2. 移除前端API密钥配置
3. 使用流式API实现实时响应效果

## 📞 技术支持

如遇到问题，请检查：

1. **API密钥配置** - 确保 `.env` 文件正确设置
2. **网络连接** - 确保能访问Google Gemini API
3. **端口占用** - 确保3001端口未被其他程序占用
4. **防火墙设置** - 确保局域网设备能访问服务器

---

**愿古代智慧与现代科技的结合，为您和您的用户指引人生方向！** 🌟 